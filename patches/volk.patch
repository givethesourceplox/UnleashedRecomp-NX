diff --git a/volk.c b/volk.c
index 90b08e9..ed8cb8c 100644
--- a/volk.c
+++ b/volk.c
@@ -13,7 +13,7 @@
 	#else
 		typedef int (__stdcall* FARPROC)(void);
 	#endif
-#else
+#elif !defined(__SWITCH__)
 #	include <dlfcn.h>
 #endif
 
@@ -72,6 +72,9 @@ static PFN_vkVoidFunction nullProcAddrStub(void* context, const char* name)
 
 VkResult volkInitialize(void)
 {
+#if defined(__SWITCH__)
+	return VK_ERROR_INITIALIZATION_FAILED;
+#else
 #if defined(_WIN32)
 	HMODULE module = LoadLibraryA("vulkan-1.dll");
 	if (!module)
@@ -114,6 +117,7 @@ VkResult volkInitialize(void)
 	volkGenLoadLoader(NULL, vkGetInstanceProcAddrStub);
 
 	return VK_SUCCESS;
+#endif
 }
 
 void volkInitializeCustom(PFN_vkGetInstanceProcAddr handler)
@@ -130,7 +134,7 @@ void volkFinalize(void)
 	{
 #if defined(_WIN32)
 		FreeLibrary((HMODULE)loadedModule);
-#else
+#elif !defined(__SWITCH__)
 		dlclose(loadedModule);
 #endif
 	}
